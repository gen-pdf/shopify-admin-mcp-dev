# Enum: `TaxSummaryCreateUserErrorCode`

Possible error codes that can be returned by `TaxSummaryCreateUserError`.

## Values

- `ORDER_NOT_FOUND` — No order was not found.
- `GENERAL_ERROR` — There was an error during enqueueing of the tax summary creation job(s).
