# Enum: `CountPrecision`

The precision of the value returned by a count field.

## Values

- `EXACT` — The count is exactly the value. A write may not be reflected instantaneously.
- `AT_LEAST` — The count is at least the value. A limit was imposed and reached.
