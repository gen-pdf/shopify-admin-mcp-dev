# Object: `MetaobjectCapabilityDataOnlineStore`

The Online Store capability for the parent metaobject.

## Fields

- `templateSuffix`: `String` — The theme template used when viewing the metaobject in a store.
