# Input Object: `ReturnApproveRequestInput`

The input fields for approving a customer's return request.

## Input Fields

- `id`: `ID!` — The ID of the return that's being approved.
- `notifyCustomer`: `Boolean` = `false` — Notify the customer when a return request is approved.
