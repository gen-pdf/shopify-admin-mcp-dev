# Enum: `MarketConditionType`

The condition types for the condition set.

## Values

- `REGION` — The condition checks the visitor's region.
- `LOCATION` — The condition checks the location that the visitor is shopping from.
- `COMPANY_LOCATION` — The condition checks the company location that the visitor is purchasing for.
