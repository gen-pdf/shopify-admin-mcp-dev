# Input Object: `MetaobjectCapabilityDataOnlineStoreInput`

The input fields for the Online Store capability to control renderability on the Online Store.

## Input Fields

- `templateSuffix`: `String` — The theme template used when viewing the metaobject in a store.
