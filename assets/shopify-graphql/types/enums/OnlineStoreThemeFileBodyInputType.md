# Enum: `OnlineStoreThemeFileBodyInputType`

The input type for a theme file body.

## Values

- `TEXT` — The text body of the theme file.
- `BASE64` — The base64 encoded body of a theme file.
- `URL` — The url of the body of a theme file.
