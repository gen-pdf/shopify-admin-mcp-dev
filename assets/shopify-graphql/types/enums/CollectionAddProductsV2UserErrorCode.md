# Enum: `CollectionAddProductsV2UserErrorCode`

Possible error codes that can be returned by `CollectionAddProductsV2UserError`.

## Values

- `CANT_ADD_TO_SMART_COLLECTION` — Can't manually add products to a smart collection.
- `COLLECTION_DOES_NOT_EXIST` — Collection doesn't exist.
