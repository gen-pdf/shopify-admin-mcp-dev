# Enum: `DiscountStatus`

The status of the discount that describes its availability,
expiration, or pending activation.

## Values

- `ACTIVE` — The discount is currently available for use.
- `EXPIRED` — The discount has reached its end date and is no longer valid.
- `SCHEDULED` — The discount is set to become active at a future date.
