# Object: `VaultPaypalBillingAgreement`

Represents a paypal billing agreement payment instrument.

## Fields

- `inactive`: `Boolean!` — Whether the paypal billing agreement is inactive.
- `name`: `String!` — The paypal account name.
- `paypalAccountEmail`: `String!` — The paypal account email address.
