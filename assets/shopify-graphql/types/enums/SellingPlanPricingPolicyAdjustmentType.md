# Enum: `SellingPlanPricingPolicyAdjustmentType`

Represents a selling plan pricing policy adjustment type.

## Values

- `PERCENTAGE` — Percentage off adjustment.
- `FIXED_AMOUNT` — Fixed amount off adjustment.
- `PRICE` — Price of the policy.
