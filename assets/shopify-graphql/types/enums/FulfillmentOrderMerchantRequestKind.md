# Enum: `FulfillmentOrderMerchantRequestKind`

The kinds of request merchants can make to a fulfillment service.

## Values

- `FULFILLMENT_REQUEST` — The merchant requests fulfillment for an `OPEN` fulfillment order.
- `CANCELLATION_REQUEST` — The merchant requests cancellation of an `IN_PROGRESS` fulfillment order.
