# Enum: `FulfillmentOrderReleaseHoldUserErrorCode`

Possible error codes that can be returned by `FulfillmentOrderReleaseHoldUserError`.

## Values

- `FULFILLMENT_ORDER_NOT_FOUND` — The fulfillment order wasn't found.
- `INVALID_ACCESS` — The app doesn't have access to release the fulfillment hold.
