# Enum: `PriceRuleTarget`

The type of lines (line_item or shipping_line) to which the price rule applies.

## Values

- `LINE_ITEM` — The price rule applies to line items.
- `SHIPPING_LINE` — The price rule applies to shipping lines.
