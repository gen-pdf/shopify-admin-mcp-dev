# Input Object: `UpdateMediaInput`

The input fields required to update a media object.

## Input Fields

- `id`: `ID!` — Specifies the media to update.
- `previewImageSource`: `String` — The source from which to update the media preview image. May be an external URL or staged upload URL.
- `alt`: `String` — The alt text associated to the media.
