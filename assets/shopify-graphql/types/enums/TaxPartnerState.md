# Enum: `TaxPartnerState`

State of the tax app configuration.

## Values

- `PENDING` — App is not configured.
- `READY` — App is configured, but not used for tax calculations.
- `ACTIVE` — App is configured and to be used for tax calculations.
