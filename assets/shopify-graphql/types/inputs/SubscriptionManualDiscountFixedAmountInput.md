# Input Object: `SubscriptionManualDiscountFixedAmountInput`

The input fields for the fixed amount value of the discount and distribution on the lines.

## Input Fields

- `amount`: `Float` — Fixed amount value.
- `appliesOnEachItem`: `Boolean` — Whether the amount is intended per line item or once per subscription.
