# Object: `ShopifyPaymentsJpChargeStatementDescriptor`

The charge descriptors for a Japanese payments account.

**Implements:** `ShopifyPaymentsChargeStatementDescriptor`

## Fields

- `default`: `String` — The default charge statement descriptor.
- `kana`: `String` *(deprecated)* — The charge statement descriptor in kana.
- `kanji`: `String` *(deprecated)* — The charge statement descriptor in kanji.
- `prefix`: `String!` — The prefix of the statement descriptor.
