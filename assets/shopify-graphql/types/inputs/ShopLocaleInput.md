# Input Object: `ShopLocaleInput`

The input fields for a shop locale.

## Input Fields

- `published`: `Boolean` — Whether the locale is published. Only published locales are visible to the buyer.
- `marketWebPresenceIds`: `[ID!]` — The market web presences on which the locale should be enabled. Pass in an empty array to remove the locale across all market web presences.
