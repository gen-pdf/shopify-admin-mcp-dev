# Enum: `CombinedListingsRole`

The role of the combined listing.

## Values

- `PARENT` — The product is the parent of a combined listing.
- `CHILD` — The product is the child of a combined listing.
