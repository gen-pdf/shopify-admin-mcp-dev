# Enum: `PaymentReminderSendUserErrorCode`

Possible error codes that can be returned by `PaymentReminderSendUserError`.

## Values

- `PAYMENT_REMINDER_SEND_UNSUCCESSFUL` — An error occurred while sending the payment reminder.
