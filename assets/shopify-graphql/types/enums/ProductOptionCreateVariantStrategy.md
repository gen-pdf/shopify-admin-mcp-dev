# Enum: `ProductOptionCreateVariantStrategy`

The set of variant strategies available for use in the `productOptionsCreate` mutation.

## Values

- `LEAVE_AS_IS` — No additional variants are created in response to the added options. Existing variants are updated with the
- `CREATE` — Existing variants are updated with the first option value of each added option. New variants are
