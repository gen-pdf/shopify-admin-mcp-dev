# Enum: `FileCreateInputDuplicateResolutionMode`

The input fields for handling if filename is already in use.

## Values

- `APPEND_UUID` — Append a UUID if filename is already in use.
- `RAISE_ERROR` — Raise an error if filename is already in use.
- `REPLACE` — Replace the existing file if filename is already in use.
