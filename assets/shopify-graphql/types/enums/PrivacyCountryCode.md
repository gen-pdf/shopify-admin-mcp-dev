# Enum: `PrivacyCountryCode`

A country code from the `ISO 3166` standard. e.g. `CA` for Canada.

## Values

- `AN` — The `ISO 3166` country code of `AN`.
- `AC` — The `ISO 3166` country code of `AC`.
- `AD` — The `ISO 3166` country code of `AD`.
- `AE` — The `ISO 3166` country code of `AE`.
- `AF` — The `ISO 3166` country code of `AF`.
- `AG` — The `ISO 3166` country code of `AG`.
- `AI` — The `ISO 3166` country code of `AI`.
- `AL` — The `ISO 3166` country code of `AL`.
- `AM` — The `ISO 3166` country code of `AM`.
- `AO` — The `ISO 3166` country code of `AO`.
- `AQ` — The `ISO 3166` country code of `AQ`.
- `AR` — The `ISO 3166` country code of `AR`.
- `AS` — The `ISO 3166` country code of `AS`.
- `AT` — The `ISO 3166` country code of `AT`.
- `AU` — The `ISO 3166` country code of `AU`.
- `AW` — The `ISO 3166` country code of `AW`.
- `AX` — The `ISO 3166` country code of `AX`.
- `AZ` — The `ISO 3166` country code of `AZ`.
- `BA` — The `ISO 3166` country code of `BA`.
- `BB` — The `ISO 3166` country code of `BB`.
- `BD` — The `ISO 3166` country code of `BD`.
- `BE` — The `ISO 3166` country code of `BE`.
- `BF` — The `ISO 3166` country code of `BF`.
- `BG` — The `ISO 3166` country code of `BG`.
- `BH` — The `ISO 3166` country code of `BH`.
- `BI` — The `ISO 3166` country code of `BI`.
- `BJ` — The `ISO 3166` country code of `BJ`.
- `BL` — The `ISO 3166` country code of `BL`.
- `BM` — The `ISO 3166` country code of `BM`.
- `BN` — The `ISO 3166` country code of `BN`.
- `BO` — The `ISO 3166` country code of `BO`.
- `BQ` — The `ISO 3166` country code of `BQ`.
- `BR` — The `ISO 3166` country code of `BR`.
- `BS` — The `ISO 3166` country code of `BS`.
- `BT` — The `ISO 3166` country code of `BT`.
- `BV` — The `ISO 3166` country code of `BV`.
- `BW` — The `ISO 3166` country code of `BW`.
- `BY` — The `ISO 3166` country code of `BY`.
- `BZ` — The `ISO 3166` country code of `BZ`.
- `CA` — The `ISO 3166` country code of `CA`.
- `CC` — The `ISO 3166` country code of `CC`.
- `CD` — The `ISO 3166` country code of `CD`.
- `CF` — The `ISO 3166` country code of `CF`.
- `CG` — The `ISO 3166` country code of `CG`.
- `CH` — The `ISO 3166` country code of `CH`.
- `CI` — The `ISO 3166` country code of `CI`.
- `CK` — The `ISO 3166` country code of `CK`.
- `CL` — The `ISO 3166` country code of `CL`.
- `CM` — The `ISO 3166` country code of `CM`.
- `CN` — The `ISO 3166` country code of `CN`.
- `CO` — The `ISO 3166` country code of `CO`.
- `CR` — The `ISO 3166` country code of `CR`.
- `CU` — The `ISO 3166` country code of `CU`.
- `CV` — The `ISO 3166` country code of `CV`.
- `CW` — The `ISO 3166` country code of `CW`.
- `CX` — The `ISO 3166` country code of `CX`.
- `CY` — The `ISO 3166` country code of `CY`.
- `CZ` — The `ISO 3166` country code of `CZ`.
- `DE` — The `ISO 3166` country code of `DE`.
- `DJ` — The `ISO 3166` country code of `DJ`.
- `DK` — The `ISO 3166` country code of `DK`.
- `DM` — The `ISO 3166` country code of `DM`.
- `DO` — The `ISO 3166` country code of `DO`.
- `DZ` — The `ISO 3166` country code of `DZ`.
- `EC` — The `ISO 3166` country code of `EC`.
- `EE` — The `ISO 3166` country code of `EE`.
- `EG` — The `ISO 3166` country code of `EG`.
- `EH` — The `ISO 3166` country code of `EH`.
- `ER` — The `ISO 3166` country code of `ER`.
- `ES` — The `ISO 3166` country code of `ES`.
- `ET` — The `ISO 3166` country code of `ET`.
- `FI` — The `ISO 3166` country code of `FI`.
- `FJ` — The `ISO 3166` country code of `FJ`.
- `FK` — The `ISO 3166` country code of `FK`.
- `FM` — The `ISO 3166` country code of `FM`.
- `FO` — The `ISO 3166` country code of `FO`.
- `FR` — The `ISO 3166` country code of `FR`.
- `GA` — The `ISO 3166` country code of `GA`.
- `GB` — The `ISO 3166` country code of `GB`.
- `GD` — The `ISO 3166` country code of `GD`.
- `GE` — The `ISO 3166` country code of `GE`.
- `GF` — The `ISO 3166` country code of `GF`.
- `GG` — The `ISO 3166` country code of `GG`.
- `GH` — The `ISO 3166` country code of `GH`.
- `GI` — The `ISO 3166` country code of `GI`.
- `GL` — The `ISO 3166` country code of `GL`.
- `GM` — The `ISO 3166` country code of `GM`.
- `GN` — The `ISO 3166` country code of `GN`.
- `GP` — The `ISO 3166` country code of `GP`.
- `GQ` — The `ISO 3166` country code of `GQ`.
- `GR` — The `ISO 3166` country code of `GR`.
- `GS` — The `ISO 3166` country code of `GS`.
- `GT` — The `ISO 3166` country code of `GT`.
- `GU` — The `ISO 3166` country code of `GU`.
- `GW` — The `ISO 3166` country code of `GW`.
- `GY` — The `ISO 3166` country code of `GY`.
- `HK` — The `ISO 3166` country code of `HK`.
- `HM` — The `ISO 3166` country code of `HM`.
- `HN` — The `ISO 3166` country code of `HN`.
- `HR` — The `ISO 3166` country code of `HR`.
- `HT` — The `ISO 3166` country code of `HT`.
- `HU` — The `ISO 3166` country code of `HU`.
- `ID` — The `ISO 3166` country code of `ID`.
- `IE` — The `ISO 3166` country code of `IE`.
- `IL` — The `ISO 3166` country code of `IL`.
- `IM` — The `ISO 3166` country code of `IM`.
- `IN` — The `ISO 3166` country code of `IN`.
- `IO` — The `ISO 3166` country code of `IO`.
- `IQ` — The `ISO 3166` country code of `IQ`.
- `IR` — The `ISO 3166` country code of `IR`.
- `IS` — The `ISO 3166` country code of `IS`.
- `IT` — The `ISO 3166` country code of `IT`.
- `JE` — The `ISO 3166` country code of `JE`.
- `JM` — The `ISO 3166` country code of `JM`.
- `JO` — The `ISO 3166` country code of `JO`.
- `JP` — The `ISO 3166` country code of `JP`.
- `KE` — The `ISO 3166` country code of `KE`.
- `KG` — The `ISO 3166` country code of `KG`.
- `KH` — The `ISO 3166` country code of `KH`.
- `KI` — The `ISO 3166` country code of `KI`.
- `KM` — The `ISO 3166` country code of `KM`.
- `KN` — The `ISO 3166` country code of `KN`.
- `KP` — The `ISO 3166` country code of `KP`.
- `KR` — The `ISO 3166` country code of `KR`.
- `KW` — The `ISO 3166` country code of `KW`.
- `KY` — The `ISO 3166` country code of `KY`.
- `KZ` — The `ISO 3166` country code of `KZ`.
- `LA` — The `ISO 3166` country code of `LA`.
- `LB` — The `ISO 3166` country code of `LB`.
- `LC` — The `ISO 3166` country code of `LC`.
- `LI` — The `ISO 3166` country code of `LI`.
- `LK` — The `ISO 3166` country code of `LK`.
- `LR` — The `ISO 3166` country code of `LR`.
- `LS` — The `ISO 3166` country code of `LS`.
- `LT` — The `ISO 3166` country code of `LT`.
- `LU` — The `ISO 3166` country code of `LU`.
- `LV` — The `ISO 3166` country code of `LV`.
- `LY` — The `ISO 3166` country code of `LY`.
- `MA` — The `ISO 3166` country code of `MA`.
- `MC` — The `ISO 3166` country code of `MC`.
- `MD` — The `ISO 3166` country code of `MD`.
- `ME` — The `ISO 3166` country code of `ME`.
- `MF` — The `ISO 3166` country code of `MF`.
- `MG` — The `ISO 3166` country code of `MG`.
- `MH` — The `ISO 3166` country code of `MH`.
- `MK` — The `ISO 3166` country code of `MK`.
- `ML` — The `ISO 3166` country code of `ML`.
- `MM` — The `ISO 3166` country code of `MM`.
- `MN` — The `ISO 3166` country code of `MN`.
- `MO` — The `ISO 3166` country code of `MO`.
- `MP` — The `ISO 3166` country code of `MP`.
- `MQ` — The `ISO 3166` country code of `MQ`.
- `MR` — The `ISO 3166` country code of `MR`.
- `MS` — The `ISO 3166` country code of `MS`.
- `MT` — The `ISO 3166` country code of `MT`.
- `MU` — The `ISO 3166` country code of `MU`.
- `MV` — The `ISO 3166` country code of `MV`.
- `MW` — The `ISO 3166` country code of `MW`.
- `MX` — The `ISO 3166` country code of `MX`.
- `MY` — The `ISO 3166` country code of `MY`.
- `MZ` — The `ISO 3166` country code of `MZ`.
- `NA` — The `ISO 3166` country code of `NA`.
- `NC` — The `ISO 3166` country code of `NC`.
- `NE` — The `ISO 3166` country code of `NE`.
- `NF` — The `ISO 3166` country code of `NF`.
- `NG` — The `ISO 3166` country code of `NG`.
- `NI` — The `ISO 3166` country code of `NI`.
- `NL` — The `ISO 3166` country code of `NL`.
- `NO` — The `ISO 3166` country code of `NO`.
- `NP` — The `ISO 3166` country code of `NP`.
- `NR` — The `ISO 3166` country code of `NR`.
- `NS` — The `ISO 3166` country code of `NS`.
- `NU` — The `ISO 3166` country code of `NU`.
- `NZ` — The `ISO 3166` country code of `NZ`.
- `OM` — The `ISO 3166` country code of `OM`.
- `PA` — The `ISO 3166` country code of `PA`.
- `PE` — The `ISO 3166` country code of `PE`.
- `PF` — The `ISO 3166` country code of `PF`.
- `PG` — The `ISO 3166` country code of `PG`.
- `PH` — The `ISO 3166` country code of `PH`.
- `PK` — The `ISO 3166` country code of `PK`.
- `PL` — The `ISO 3166` country code of `PL`.
- `PM` — The `ISO 3166` country code of `PM`.
- `PN` — The `ISO 3166` country code of `PN`.
- `PR` — The `ISO 3166` country code of `PR`.
- `PS` — The `ISO 3166` country code of `PS`.
- `PT` — The `ISO 3166` country code of `PT`.
- `PW` — The `ISO 3166` country code of `PW`.
- `PY` — The `ISO 3166` country code of `PY`.
- `QA` — The `ISO 3166` country code of `QA`.
- `RE` — The `ISO 3166` country code of `RE`.
- `RO` — The `ISO 3166` country code of `RO`.
- `RS` — The `ISO 3166` country code of `RS`.
- `RU` — The `ISO 3166` country code of `RU`.
- `RW` — The `ISO 3166` country code of `RW`.
- `SA` — The `ISO 3166` country code of `SA`.
- `SB` — The `ISO 3166` country code of `SB`.
- `SC` — The `ISO 3166` country code of `SC`.
- `SD` — The `ISO 3166` country code of `SD`.
- `SE` — The `ISO 3166` country code of `SE`.
- `SG` — The `ISO 3166` country code of `SG`.
- `SH` — The `ISO 3166` country code of `SH`.
- `SI` — The `ISO 3166` country code of `SI`.
- `SJ` — The `ISO 3166` country code of `SJ`.
- `SK` — The `ISO 3166` country code of `SK`.
- `SL` — The `ISO 3166` country code of `SL`.
- `SM` — The `ISO 3166` country code of `SM`.
- `SN` — The `ISO 3166` country code of `SN`.
- `SO` — The `ISO 3166` country code of `SO`.
- `SR` — The `ISO 3166` country code of `SR`.
- `SS` — The `ISO 3166` country code of `SS`.
- `ST` — The `ISO 3166` country code of `ST`.
- `SV` — The `ISO 3166` country code of `SV`.
- `SX` — The `ISO 3166` country code of `SX`.
- `SY` — The `ISO 3166` country code of `SY`.
- `SZ` — The `ISO 3166` country code of `SZ`.
- `TA` — The `ISO 3166` country code of `TA`.
- `TC` — The `ISO 3166` country code of `TC`.
- `TD` — The `ISO 3166` country code of `TD`.
- `TF` — The `ISO 3166` country code of `TF`.
- `TG` — The `ISO 3166` country code of `TG`.
- `TH` — The `ISO 3166` country code of `TH`.
- `TJ` — The `ISO 3166` country code of `TJ`.
- `TK` — The `ISO 3166` country code of `TK`.
- `TL` — The `ISO 3166` country code of `TL`.
- `TM` — The `ISO 3166` country code of `TM`.
- `TN` — The `ISO 3166` country code of `TN`.
- `TO` — The `ISO 3166` country code of `TO`.
- `TR` — The `ISO 3166` country code of `TR`.
- `TT` — The `ISO 3166` country code of `TT`.
- `TV` — The `ISO 3166` country code of `TV`.
- `TW` — The `ISO 3166` country code of `TW`.
- `TZ` — The `ISO 3166` country code of `TZ`.
- `UA` — The `ISO 3166` country code of `UA`.
- `UG` — The `ISO 3166` country code of `UG`.
- `UM` — The `ISO 3166` country code of `UM`.
- `US` — The `ISO 3166` country code of `US`.
- `UY` — The `ISO 3166` country code of `UY`.
- `UZ` — The `ISO 3166` country code of `UZ`.
- `VA` — The `ISO 3166` country code of `VA`.
- `VC` — The `ISO 3166` country code of `VC`.
- `VE` — The `ISO 3166` country code of `VE`.
- `VG` — The `ISO 3166` country code of `VG`.
- `VI` — The `ISO 3166` country code of `VI`.
- `VN` — The `ISO 3166` country code of `VN`.
- `VU` — The `ISO 3166` country code of `VU`.
- `WF` — The `ISO 3166` country code of `WF`.
- `WS` — The `ISO 3166` country code of `WS`.
- `XK` — The `ISO 3166` country code of `XK`.
- `YE` — The `ISO 3166` country code of `YE`.
- `YT` — The `ISO 3166` country code of `YT`.
- `ZA` — The `ISO 3166` country code of `ZA`.
- `ZM` — The `ISO 3166` country code of `ZM`.
- `ZW` — The `ISO 3166` country code of `ZW`.
- `XX` — The `ISO 3166` country code of `XX`.
