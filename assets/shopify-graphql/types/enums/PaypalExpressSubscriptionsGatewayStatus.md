# Enum: `PaypalExpressSubscriptionsGatewayStatus`

Represents a valid PayPal Express subscriptions gateway status.

## Values

- `ENABLED` — The status is enabled.
- `DISABLED` — The status is disabled.
- `PENDING` — The status is pending.
