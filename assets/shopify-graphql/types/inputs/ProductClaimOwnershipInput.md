# Input Object: `ProductClaimOwnershipInput`

The input fields to claim ownership for Product features such as Bundles.

## Input Fields

- `bundles`: `Boolean` — Claiming ownership of bundles lets the app render a custom UI for the bundles' card on the
