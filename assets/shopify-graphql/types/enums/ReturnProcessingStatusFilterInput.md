# Enum: `ReturnProcessingStatusFilterInput`

Filter line items based on processing status.

## Values

- `PROCESSED` — Only include line items that have been processed.
- `PROCESSABLE` — Only include line items that have some processable quantity.
