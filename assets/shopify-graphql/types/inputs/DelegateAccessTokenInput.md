# Input Object: `DelegateAccessTokenInput`

The input fields for a delegate access token.

## Input Fields

- `delegateAccessScope`: `[String!]!` — The list of scopes that will be delegated to the new access token.
- `expiresIn`: `Int` — The amount of time, in seconds, after which the delegate access token is no longer valid.
