# Enum: `QuantityRuleOriginType`

The origin of quantity rule on a price list.

## Values

- `FIXED` — Quantity rule is explicitly defined.
- `RELATIVE` — Quantity rule falls back to the relative rule.
