name: Bug Report
description: Report a bug with the Shopify Admin GraphQL MCP server
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to report a bug. Please fill out the sections below so we can reproduce and fix the issue.

  - type: dropdown
    id: mcp-client
    attributes:
      label: MCP Client
      description: Which MCP client are you using?
      options:
        - Claude Desktop
        - Claude Code (CLI)
        - Cursor
        - Windsurf
        - VS Code (Copilot)
        - Other (please specify below)
    validations:
      required: true

  - type: input
    id: mcp-client-version
    attributes:
      label: MCP Client Version
      description: Version of your MCP client (e.g. Claude Desktop 1.2.3)
      placeholder: e.g. Claude Desktop 1.2.3
    validations:
      required: false

  - type: dropdown
    id: auth-type
    attributes:
      label: Authentication Type
      description: How is the server authenticating with Shopify?
      options:
        - Admin access token
    validations:
      required: true

  - type: textarea
    id: config
    attributes:
      label: MCP Server Configuration
      description: Paste your MCP server config (redact any tokens or secrets).
      render: json
      placeholder: |
        {
          "mcpServers": {
            "shopify-admin-graphql": {
              "command": "...",
              "args": ["..."]
            }
          }
        }
    validations:
      required: true

  - type: input
    id: endpoint
    attributes:
      label: Failing Endpoint / Tool
      description: Which Shopify GraphQL operation or MCP tool was being called when the error occurred?
      placeholder: e.g. productCreate, products, search, or execute
    validations:
      required: false

  - type: textarea
    id: description
    attributes:
      label: Bug Description
      description: A clear description of the bug.
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Steps to Reproduce
      description: Step-by-step instructions to reproduce the issue.
      placeholder: |
        1. Configure MCP server with ...
        2. Authenticate using ...
        3. Call tool / make request ...
        4. See error
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected Behavior
      description: What did you expect to happen?
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: Actual Behavior
      description: What actually happened? Include any error messages.
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: Relevant Logs / Error Output
      description: Paste any relevant logs or error output. Redact sensitive information.
      render: shell
    validations:
      required: false

  - type: textarea
    id: additional
    attributes:
      label: Additional Context
      description: Any other context, screenshots, or information that might help.
    validations:
      required: false
