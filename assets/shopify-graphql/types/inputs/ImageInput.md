# Input Object: `ImageInput`

The input fields for an image.

## Input Fields

- `id`: `ID` — A globally-unique ID.
- `altText`: `String` — A word or phrase to share the nature or contents of an image.
- `src`: `String` — The URL of the image. May be a staged upload URL.
