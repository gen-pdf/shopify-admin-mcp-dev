# Enum: `BulkOperationsSortKeys`

The set of valid sort keys for the BulkOperations query.

## Values

- `COMPLETED_AT` — Sort by the `completed_at` value.
- `CREATED_AT` — Sort by the `created_at` value.
