# Input Object: `ProductPublicationInput`

The input fields for specifying a publication to which a product will be published.

## Input Fields

- `publicationId`: `ID` — ID of the publication.
- `publishDate`: `DateTime` — The date and time that the product was (or will be) published.
