# Input Object: `SavedSearchUpdateInput`

The input fields to update a saved search.

## Input Fields

- `id`: `ID!` — ID of the saved search to update.
- `name`: `String` — A descriptive name of the saved search.
- `query`: `String` — The query string of a saved search. This included search terms and filters.
