# Enum: `MarketConditionApplicationType`

The application level for a market condition.

## Values

- `SPECIFIED` — The condition matches specified records of a given type.
- `ALL` — The condition matches all records of a given type.
