# Input Object: `MetafieldIdentifierInput`

The input fields that identify metafields.

## Input Fields

- `ownerId`: `ID!` — The unique ID of the resource that the metafield is attached to.
- `namespace`: `String!` — The namespace of the metafield.
- `key`: `String!` — The key of the metafield.
