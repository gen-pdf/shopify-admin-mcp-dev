# Input Object: `InventoryLevelInput`

The input fields for an inventory level.

## Input Fields

- `availableQuantity`: `Int!` — The available quantity of an inventory item at a location.
- `locationId`: `ID!` — The ID of a location associated with the inventory level.
