# Input Object: `OrderCreateCustomAttributeInput`

The input fields for a note attribute for an order.

## Input Fields

- `key`: `String!` — The key or name of the custom attribute.
- `value`: `String!` — The value of the custom attribute.
