# Enum: `BulkOperationErrorCode`

Error codes for failed bulk operations.

## Values

- `ACCESS_DENIED` — The provided operation `query` returned access denied due to missing
- `INTERNAL_SERVER_ERROR` — The operation resulted in partial or incomplete data due to internal server errors during execution.
- `TIMEOUT` — The operation resulted in partial or incomplete data due to query timeouts during execution.
