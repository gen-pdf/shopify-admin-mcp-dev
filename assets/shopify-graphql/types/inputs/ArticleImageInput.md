# Input Object: `ArticleImageInput`

The input fields for an image associated with an article.

## Input Fields

- `altText`: `String` — A word or phrase to share the nature or contents of an image.
- `url`: `String` — The URL of the image.
