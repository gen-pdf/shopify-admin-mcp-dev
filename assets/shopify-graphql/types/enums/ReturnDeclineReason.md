# Enum: `ReturnDeclineReason`

The reason why the merchant declined a customer's return request.

## Values

- `RETURN_PERIOD_ENDED` — The return period has ended.
- `FINAL_SALE` — The return contains final sale items.
- `OTHER` — The return is declined for another reason.
