# Enum: `DiscountApplicationLevel`

The level at which the discount's value is applied.

## Values

- `ORDER` — The discount is applied at the order level.
- `LINE` — The discount is applied at the line level.
