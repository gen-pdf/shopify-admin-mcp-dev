# Object: `MediaImageOriginalSource`

The original source for an image.

## Fields

- `fileSize`: `Int` — The size of the original file in bytes.
- `url`: `URL` — The URL of the original image, valid only for a short period.
