# Input Object: `SubscriptionManualDiscountLinesInput`

The input fields for line items that the discount refers to.

## Input Fields

- `add`: `[ID!]` — The ID of the lines to add to the subscription discount.
- `remove`: `[ID!]` — The ID of the lines to remove from the subscription discount.
