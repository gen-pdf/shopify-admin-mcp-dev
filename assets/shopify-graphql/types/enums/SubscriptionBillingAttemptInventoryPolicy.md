# Enum: `SubscriptionBillingAttemptInventoryPolicy`

The inventory policy for a billing attempt.

## Values

- `PRODUCT_VARIANT_INVENTORY_POLICY` — Respect the merchant's product variant
- `ALLOW_OVERSELLING` — Override the merchant's product variant
