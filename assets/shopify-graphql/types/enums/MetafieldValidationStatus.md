# Enum: `MetafieldValidationStatus`

Possible metafield validation statuses.

## Values

- `ANY` — Any validation status (valid or invalid).
- `VALID` — Valid (according to definition).
- `INVALID` — Invalid (according to definition).
