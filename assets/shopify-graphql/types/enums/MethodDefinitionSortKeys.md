# Enum: `MethodDefinitionSortKeys`

The set of valid sort keys for the MethodDefinition query.

## Values

- `ID` — Sort by the `id` value.
- `RATE_PROVIDER_TYPE` — Sort by the `rate_provider_type` value.
