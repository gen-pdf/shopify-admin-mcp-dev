# Enum: `MarketLocalizableResourceType`

The type of resources that are market localizable.

## Values

- `METAFIELD` — A metafield. Market localizable fields: `value`.
- `METAOBJECT` — A Metaobject. Market Localizable fields are determined by the Metaobject type.
