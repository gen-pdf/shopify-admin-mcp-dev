# Object: `SubscriptionBillingAttempt`

A record of an execution of the subscription billing process. Billing attempts use idempotency keys to avoid duplicate order creation.

When a billing attempt completes successfully, it creates an [`Order`](https://shopify.dev/docs/api/admin-graphql/latest/objects/Order). The attempt includes associated payment transactions and any errors that occur during billing. If 3D Secure authentication is required, the `nextActionUrl` field provides the redirect URL for customer verification.

**Implements:** `Node`

## Fields

- `completedAt`: `DateTime` — The date and time when the billing attempt was completed.
- `createdAt`: `DateTime!` — The date and time when the billing attempt was created.
- `errorCode`: `SubscriptionBillingAttemptErrorCode` *(deprecated)* — A code corresponding to a payment error during processing.
- `errorMessage`: `String` *(deprecated)* — A message describing a payment error during processing.
- `id`: `ID!` — A globally-unique ID.
- `idempotencyKey`: `String!` — A unique key generated by the client to avoid duplicate payments.
- `nextActionUrl`: `URL` *(deprecated)* — The URL where the customer needs to be redirected so they can complete the 3D Secure payment flow.
- `order`: `Order` *(deprecated)* — The result of this billing attempt if completed successfully.
- `originTime`: `DateTime` — The date and time used to calculate fulfillment intervals for a billing attempt that
- `paymentGroupId`: `String` — The reference shared between retried payment attempts.
- `paymentSessionId`: `String` — The reference shared between payment attempts with similar payment details.
- `processingError`: `SubscriptionBillingAttemptProcessingError` *(deprecated)* — Error information from processing the billing attempt.
- `ready`: `Boolean!` *(deprecated)* — Whether the billing attempt is still processing.
- `respectInventoryPolicy`: `Boolean!` — Whether the billing attempt respects the merchant's inventory policy.
- `subscriptionContract`: `SubscriptionContract!` — The subscription contract.
- `transactions` `(first, after, last, before, reverse)`: `OrderTransactionConnection!` — The transactions created by the billing attempt.

## Related Types

- [Order](../../types/objects/Order.md)
- [SubscriptionBillingAttemptErrorCode](../../types/enums/SubscriptionBillingAttemptErrorCode.md)
- [SubscriptionBillingAttemptProcessingError](../../types/interfaces/SubscriptionBillingAttemptProcessingError.md)
- [SubscriptionContract](../../types/objects/SubscriptionContract.md)
