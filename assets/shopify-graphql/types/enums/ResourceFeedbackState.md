# Enum: `ResourceFeedbackState`

The state of the resource feedback.

## Values

- `ACCEPTED` — No action required from merchant.
- `REQUIRES_ACTION` — The merchant needs to resolve an issue with the resource.
