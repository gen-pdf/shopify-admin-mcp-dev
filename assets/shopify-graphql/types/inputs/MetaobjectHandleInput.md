# Input Object: `MetaobjectHandleInput`

The input fields for retrieving a metaobject by handle.

## Input Fields

- `type`: `String!` — The type of the metaobject. Must match an existing metaobject definition type.
- `handle`: `String!` — The handle of the metaobject to create or update.
