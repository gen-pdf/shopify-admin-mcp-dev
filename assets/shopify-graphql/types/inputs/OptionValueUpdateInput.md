# Input Object: `OptionValueUpdateInput`

The input fields for updating a product option value.

## Input Fields

- `id`: `ID!` — Specifies the product option value.
- `name`: `String` — Value associated with an option.
- `linkedMetafieldValue`: `String` — Metafield value associated with an option.
