# Object: `DeliveryProductVariantsCount`

How many product variants are in a profile. This count is capped at 500.

## Fields

- `capped`: `Boolean!` — Whether the count has reached the cap of 500.
- `count`: `Int!` — The product variant count.
