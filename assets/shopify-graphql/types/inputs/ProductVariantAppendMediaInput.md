# Input Object: `ProductVariantAppendMediaInput`

The input fields required to append media to a single variant.

## Input Fields

- `variantId`: `ID!` — Specifies the variant to which media will be appended.
- `mediaIds`: `[ID!]!` — Specifies the media to append to the variant.
