# Input Object: `ThemeFilesCopyFileInput`

The input fields for the file copy.

## Input Fields

- `dstFilename`: `String!` — The new file where the content is copied to.
- `srcFilename`: `String!` — The source file to copy from.
