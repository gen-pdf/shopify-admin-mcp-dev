# Input Object: `LinkedMetafieldCreateInput`

The input fields required to link a product option to a metafield.

## Input Fields

- `namespace`: `String!` — The namespace of the metafield this option is linked to.
- `key`: `String!` — The key of the metafield this option is linked to.
- `values`: `[String!]` — Values associated with the option.
