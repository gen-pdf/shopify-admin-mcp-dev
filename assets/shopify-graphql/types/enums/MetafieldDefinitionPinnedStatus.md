# Enum: `MetafieldDefinitionPinnedStatus`

Possible metafield definition pinned statuses.

## Values

- `ANY` — All metafield definitions.
- `PINNED` — Only metafield definitions that are pinned.
- `UNPINNED` — Only metafield definitions that are not pinned.
