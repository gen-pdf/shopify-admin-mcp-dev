# Input Object: `SelectedVariantOptionInput`

The input fields for the selected variant option of the combined listing.

## Input Fields

- `name`: `String!` — The name of the parent product's option.
- `value`: `String!` — The selected option value of the parent product's option.
- `linkedMetafieldValue`: `String` — The metaobject value of the linked metafield.
