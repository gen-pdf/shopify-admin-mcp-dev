# Enum: `SaleActionType`

The possible order action types for a sale.

## Values

- `ORDER` — A purchase or charge.
- `RETURN` — A removal or return.
- `UPDATE` — A change to the price, taxes, or discounts for a prior purchase.
- `UNKNOWN` — An unknown order action. Represents new actions that may be added in future versions.
