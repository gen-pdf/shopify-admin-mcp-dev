# Enum: `StaffMembersSortKeys`

The set of valid sort keys for the StaffMembers query.

## Values

- `EMAIL` — Sort by the `email` value.
- `FIRST_NAME` — Sort by the `first_name` value.
- `ID` — Sort by the `id` value.
- `LAST_NAME` — Sort by the `last_name` value.
