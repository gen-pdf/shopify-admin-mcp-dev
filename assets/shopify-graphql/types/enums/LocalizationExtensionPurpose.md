# Enum: `LocalizationExtensionPurpose`

The purpose of a localization extension.

## Values

- `SHIPPING` — Extensions that are used for shipping purposes, for example, customs clearance.
- `TAX` — Extensions that are used for taxes purposes, for example, invoicing.
