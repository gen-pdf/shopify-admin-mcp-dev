# Enum: `ShopPayPaymentRequestReceiptsSortKeys`

The set of valid sort keys for the ShopPayPaymentRequestReceipts query.

## Values

- `CREATED_AT` — Sort by the `created_at` value.
- `ID` — Sort by the `id` value.
