# Input Object: `DiscountCombinesWithInput`

The input fields to determine which discount classes the discount can combine with.

## Input Fields

- `productDiscounts`: `Boolean` = `false` — Whether the discount combines with the
- `orderDiscounts`: `Boolean` = `false` — Whether the discount combines with the
- `shippingDiscounts`: `Boolean` = `false` — Whether the discount combines
