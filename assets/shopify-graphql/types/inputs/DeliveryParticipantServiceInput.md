# Input Object: `DeliveryParticipantServiceInput`

The input fields for a shipping service provided by a participant.

## Input Fields

- `name`: `String!` — The name of the service.
- `active`: `Boolean!` — Whether the service is active.
