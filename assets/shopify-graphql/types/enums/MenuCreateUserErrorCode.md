# Enum: `MenuCreateUserErrorCode`

Possible error codes that can be returned by `MenuCreateUserError`.

## Values

- `NOT_FOUND` — The record with the ID used as the input value couldn't be found.
- `NESTING_TOO_DEEP` — The menu cannot be nested more than 3 level deep.
