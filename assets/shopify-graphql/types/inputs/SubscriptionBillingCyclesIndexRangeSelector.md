# Input Object: `SubscriptionBillingCyclesIndexRangeSelector`

The input fields to select a subset of subscription billing cycles within an index range.

## Input Fields

- `startIndex`: `Int!` — The start index for the range.
- `endIndex`: `Int!` — The end index for the range.
