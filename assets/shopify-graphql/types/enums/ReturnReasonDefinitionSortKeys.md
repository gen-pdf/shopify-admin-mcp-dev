# Enum: `ReturnReasonDefinitionSortKeys`

The set of valid sort keys for the ReturnReasonDefinition query.

## Values

- `HANDLE` — Sort by the `handle` value.
- `ID` — Sort by the `id` value.
- `NAME` — Sort by the `name` value.
