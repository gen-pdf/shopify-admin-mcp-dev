# Object: `SegmentMembership`

The response type for the `segmentMembership` object.

## Fields

- `isMember`: `Boolean!` — A Boolean that indicates whether or not the customer in the query is a member of the segment, which is identified using the `segmentId`.
- `segmentId`: `ID!` — A `segmentId` that's used for testing membership.
