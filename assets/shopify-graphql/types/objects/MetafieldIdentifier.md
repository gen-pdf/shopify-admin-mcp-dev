# Object: `MetafieldIdentifier`

Identifies a metafield by its owner resource, namespace, and key.

## Fields

- `key`: `String!` — The key of the metafield.
- `namespace`: `String!` — The namespace of the metafield.
- `ownerId`: `ID!` — GID of the owner resource that the metafield belongs to.
