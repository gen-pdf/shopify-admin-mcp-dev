# Enum: `PriceRuleAllocationMethod`

The method by which the price rule's value is allocated to its entitled items.

## Values

- `EACH` — The value will be applied to each of the entitled items.
- `ACROSS` — The value will be applied once across the entitled items.
