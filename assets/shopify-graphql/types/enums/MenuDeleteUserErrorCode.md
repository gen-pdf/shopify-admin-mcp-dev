# Enum: `MenuDeleteUserErrorCode`

Possible error codes that can be returned by `MenuDeleteUserError`.

## Values

- `MENU_DOES_NOT_EXIST` — Menu does not exist.
- `UNABLE_TO_DELETE_DEFAULT_MENU` — Default menu cannot be deleted.
