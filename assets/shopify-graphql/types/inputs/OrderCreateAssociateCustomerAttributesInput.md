# Input Object: `OrderCreateAssociateCustomerAttributesInput`

The input fields for identifying an existing customer to associate with the order.

## Input Fields

- `id`: `ID` — The customer to associate to the order.
- `email`: `String` — The email of the customer to associate to the order.
