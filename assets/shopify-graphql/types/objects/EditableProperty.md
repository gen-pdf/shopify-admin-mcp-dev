# Object: `EditableProperty`

The attribute editable information.

## Fields

- `locked`: `Boolean!` — Whether the attribute is locked for editing.
- `reason`: `FormattedString` — The reason the attribute is locked for editing.
