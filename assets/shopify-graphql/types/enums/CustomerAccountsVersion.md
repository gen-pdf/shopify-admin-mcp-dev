# Enum: `CustomerAccountsVersion`

The login redirection target for customer accounts.

## Values

- `CLASSIC` — The customer is redirected to the classic customer accounts login page.
- `NEW_CUSTOMER_ACCOUNTS` — The customer is redirected to the new customer accounts login page.
