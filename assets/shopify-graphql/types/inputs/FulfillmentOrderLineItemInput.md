# Input Object: `FulfillmentOrderLineItemInput`

The input fields used to include the quantity of the fulfillment order line item that should be fulfilled.

## Input Fields

- `id`: `ID!` — The ID of the fulfillment order line item.
- `quantity`: `Int!` — The quantity of the fulfillment order line item.
