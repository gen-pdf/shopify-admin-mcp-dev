# Input Object: `SubscriptionBillingCycleSelector`

The input fields to select SubscriptionBillingCycle by either date or index. Both past and future billing cycles can be selected.

## Input Fields

- `index`: `Int` — Returns a billing cycle by index.
- `date`: `DateTime` — Returns a billing cycle by date.
