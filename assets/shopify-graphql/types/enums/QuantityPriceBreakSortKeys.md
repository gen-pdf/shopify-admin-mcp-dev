# Enum: `QuantityPriceBreakSortKeys`

The set of valid sort keys for the QuantityPriceBreak query.

## Values

- `ID` — Sort by the `id` value.
- `MINIMUM_QUANTITY` — Sort by the `minimum_quantity` value.
