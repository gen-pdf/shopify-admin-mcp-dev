# Enum: `BadgeType`

The possible types for a badge.

## Values

- `DEFAULT` — This badge has type `default`.
- `SUCCESS` — This badge has type `success`.
- `ATTENTION` — This badge has type `attention`.
- `WARNING` — This badge has type `warning`.
- `INFO` — This badge has type `info`.
- `CRITICAL` — This badge has type `critical`.
