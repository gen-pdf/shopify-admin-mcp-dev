# Enum: `LocalizedFieldPurpose`

The purpose of a localized field.

## Values

- `SHIPPING` — Fields that are used for shipping purposes, for example, customs clearance.
- `TAX` — Fields that are used for taxes purposes, for example, invoicing.
