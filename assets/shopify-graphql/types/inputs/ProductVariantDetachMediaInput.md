# Input Object: `ProductVariantDetachMediaInput`

The input fields required to detach media from a single variant.

## Input Fields

- `variantId`: `ID!` — Specifies the variant from which media will be detached.
- `mediaIds`: `[ID!]!` — Specifies the media to detach from the variant.
