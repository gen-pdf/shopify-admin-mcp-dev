# Input Object: `IncomingRequestLineItemInput`

The input fields for the incoming line item.

## Input Fields

- `fulfillmentOrderLineItemId`: `ID!` — The ID of the rejected line item.
- `message`: `String` — The rejection message of the line item.
