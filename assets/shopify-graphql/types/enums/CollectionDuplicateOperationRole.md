# Enum: `CollectionDuplicateOperationRole`

The role a collection plays in a duplication operation.

## Values

- `SOURCE` — Products are being duplicated from this collection.
- `TARGET` — Products are being duplicated onto this collection.
